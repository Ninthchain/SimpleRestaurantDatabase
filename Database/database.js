const mongodb = require('mongodb')

class Database
{
    client
    /**
     * 
     * @param {string} url 
     */
    constructor(url)
    {
        this.client = new mongodb.MongoClient(address)
        
    }

    function start()
    {

    }
}